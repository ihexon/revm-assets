name: build

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main", "dev" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build_krun_krunfw_darwin:
    needs: build_krun_krunfw_linux
    strategy:
      fail-fast: true
      matrix:
        os: [ "macos-14" ]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build libkrun
        shell: bash
        run: |
          ./build_libkrun.sh

      - name: download libkrunfw-src-Linux-aarch64.tar.zst tree
        uses: actions/download-artifact@v5
        if: ${{ matrix.os == 'macos-14' }}
        with:
          name: libkrunfw-src-Linux-aarch64.tar.zst

      - name: Build libkrunfw
        shell: bash
        run: |
          ./build_libkrunfw.sh

      - name: upload libkrunfw-Darwin-arm64.tar.zst
        uses: actions/upload-artifact@v4
        if: ${{ matrix.os == 'macos-14' }}
        with:
          name: libkrunfw-Darwin-arm64.tar.zst
          path: libkrunfw-Darwin-arm64.tar.zst
          retention-days: 1
          compression-level: 0
          overwrite: true

      - name: upload libkrun-Darwin-arm64.tar.zst
        uses: actions/upload-artifact@v4
        if: ${{ matrix.os == 'macos-14' }}
        with:
          name: libkrun-Darwin-arm64.tar.zst
          path: libkrun-Darwin-arm64.tar.zst
          retention-days: 1
          compression-level: 0
          overwrite: true

  build_krun_krunfw_linux:
    strategy:
      fail-fast: true
      matrix:
        os: [ "ubuntu-22.04","ubuntu-22.04-arm" ]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build libkrun
        shell: bash
        run: |
          ./build_libkrun.sh

      - name: Build libkrunfw
        shell: bash
        run: |
          ./build_libkrunfw.sh
      # Only upload libkrunfw-src-Linux-aarch64.tar.zst
      - name: upload libkrunfw-src-Linux-aarch64.tar.zst
        uses: actions/upload-artifact@v4
        if: ${{ matrix.os == 'ubuntu-22.04-arm' }}
        with:
          name: libkrunfw-src-Linux-aarch64.tar.zst
          path: libkrunfw-src-Linux-aarch64.tar.zst
          retention-days: 1
          compression-level: 0
          overwrite: true

      - name: upload libkrunfw-Linux-x86_64.tar.zst
        uses: actions/upload-artifact@v4
        if: ${{ matrix.os == 'ubuntu-22.04' }}
        with:
          name: libkrunfw-Linux-x86_64.tar.zst
          path: libkrunfw-Linux-x86_64.tar.zst
          retention-days: 1
          compression-level: 0
          overwrite: true

      - name: upload libkrunfw-Linux-aarch64.tar.zst
        uses: actions/upload-artifact@v4
        if: ${{ matrix.os == 'ubuntu-22.04-arm' }}
        with:
          name: libkrunfw-Linux-aarch64.tar.zst
          path: libkrunfw-Linux-aarch64.tar.zst
          retention-days: 1
          compression-level: 0
          overwrite: true

      - name: upload libkrun-Linux-aarch64.tar.zst
        uses: actions/upload-artifact@v4
        if: ${{ matrix.os == 'ubuntu-22.04-arm' }}
        with:
          name: libkrun-Linux-aarch64.tar.zst
          path: libkrun-Linux-aarch64.tar.zst
          retention-days: 1
          compression-level: 0
          overwrite: true

      - name: upload libkrun-Linux-x86_64.tar.zst
        uses: actions/upload-artifact@v4
        if: ${{ matrix.os == 'ubuntu-22.04' }}
        with:
          name: libkrun-Linux-x86_64.tar.zst
          path: libkrun-Linux-x86_64.tar.zst
          retention-days: 1
          compression-level: 0
          overwrite: true

  build_darwin_tools:
    strategy:
      fail-fast: true
      matrix:
        os: [ "macos-14" ]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build e2fsprogs
        run: |
          ./build_e2fsprogs.sh

      - name: upload e2fsprogs-Darwin-arm64.tar.zst
        uses: actions/upload-artifact@v4
        with:
          name: e2fsprogs-Darwin-arm64.tar.zst
          path: e2fsprogs-Darwin-arm64.tar.zst
          retention-days: 1
          compression-level: 0
          overwrite: true


  build_linux_tools:
    strategy:
      fail-fast: true
      matrix:
        os: [ "ubuntu-22.04","ubuntu-22.04-arm" ]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build static Busybox
        shell: bash
        run: |
          ./build_busybox.sh

      - name: Build static Dropbear
        shell: sh
        run: |
          docker run --rm --workdir "$(pwd)" -v "$(pwd):$(pwd)" alpine:edge sh -c "$(pwd)/build_dropbear.sh"

      - name: upload busybox-Linux-x86_64.tar.zst
        uses: actions/upload-artifact@v4
        if: ${{ matrix.os == 'ubuntu-22.04' }}
        with:
          name: busybox-Linux-x86_64.tar.zst
          path: busybox-Linux-x86_64.tar.zst
          retention-days: 1
          compression-level: 0
          overwrite: true

      - name: upload busybox-Linux-aarch64.tar.zst
        uses: actions/upload-artifact@v4
        if: ${{ matrix.os == 'ubuntu-22.04-arm' }}
        with:
          name: busybox-Linux-aarch64.tar.zst
          path: busybox-Linux-aarch64.tar.zst
          retention-days: 1
          compression-level: 0
          overwrite: true

      - name: upload dropbear-Linux-x86_64.tar.zst
        uses: actions/upload-artifact@v4
        if: ${{ matrix.os == 'ubuntu-22.04' }}
        with:
          name: dropbear-Linux-x86_64.tar.zst
          path: dropbear-Linux-x86_64.tar.zst
          retention-days: 1
          compression-level: 0
          overwrite: true

      - name: upload dropbear-Linux-aarch64.tar.zst
        uses: actions/upload-artifact@v4
        if: ${{ matrix.os == 'ubuntu-22.04-arm' }}
        with:
          name: dropbear-Linux-aarch64.tar.zst
          path: dropbear-Linux-aarch64.tar.zst
          retention-days: 1
          compression-level: 0
          overwrite: true
